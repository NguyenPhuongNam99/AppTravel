import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 60,
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 5,
  },
  title: {
    fontWeight: 'bold',
    textAlign: 'center',
    flex: 1,
  },
});
export default styles;
